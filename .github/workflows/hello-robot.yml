name: Say Hello!

on:
  workflow_dispatch:

jobs:
  say-hello:
    runs-on: ubuntu-latest 

    steps:
    - name: Hello
      run: echo "Say Hello to Github Actions!"

    - name: show Time
      run: date
